/* globals get exported to be accessed from other modules */

export const cone = {
    // viewport state is defined by window width
    viewportState: null,
    sidebar_menu: {
        exists: false,
        unload: null
    }
}
