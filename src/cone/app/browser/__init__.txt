browser
-------

Import root and create dummy request::

    >>> from cone.app import root
    >>> from pyramid.testing import DummyRequest
    >>> request = DummyRequest()

Test render_main_template::

    >>> from cone.app.browser import render_main_template
    >>> render_main_template(root, request)
    <Response at ... 200 OK>

Test default view callable::

    >>> from cone.app.browser import main
    >>> main(root, request)
    <Response at ... 200 OK>

Test login view callable::

    >>> from cone.app.browser import login
    >>> login(root, request)
    <Response at ... 200 OK>

Test logout view callable::

    >>> from cone.app.browser import logout
    >>> logout(root, request)
    <HTTPFound at ... 302 Found>